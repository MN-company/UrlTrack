services:
  ulrtrack:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      DATABASE_URL: sqlite:////app/server/data/ulrtrack.db
    volumes:
      - ./server/data:/app/server/data
